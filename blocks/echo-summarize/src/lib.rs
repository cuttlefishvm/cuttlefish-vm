//! Example proc-block: summarizes one local file.
//!
//! Stub: the block implementation lands here next.
//!
//! This is a fixture, not a library — the host's integration tests build it to
//! `wasm32-unknown-unknown` and drive it through a real wasm boundary, which is
//! what makes those tests exercise the actual ABI rather than a mock of it.
